# SkyCAIR OS — SkySHIELD-ATF DNS Management
# NetworkManager: dns=none — do NOT overwrite /etc/resolv.conf
# DNS is managed by dnscrypt-proxy (encrypted DoH/DNSCrypt)
# Resolvers: Cloudflare 1.1.1.1 + Quad9 9.9.9.9 — no Google DNS
[main]
dns=none
